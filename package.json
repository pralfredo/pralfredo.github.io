{
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.1.12"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d ./dist"
  }
}
